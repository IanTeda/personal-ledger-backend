# Environment file for local development and CI checks.
#
# This file provides example environment variables for the Personal Ledger backend.
# Copy this file to `.env` and modify as needed for your local development environment.
#
# IMPORTANT: Do not commit production credentials to this file. Use CI secrets or
# a local `.env` file kept out of version control for production deployments.

# =============================================================================
# SQLx Configuration (for compile-time checks and migrations)
# =============================================================================

# SQLite database connection URL for SQLx compile-time query validation.
# This is used by SQLx during build and for running migrations.
DATABASE_URL="sqlite:.personal-ledger-dev.db"

# =============================================================================
# Application Configuration (overrides config/ledger-backend.conf)
# =============================================================================

# All application settings can be overridden using environment variables
# prefixed with `LEDGER_BACKEND_`. Format: LEDGER_BACKEND_<SECTION>_<KEY>
#
# Example: To override `server.address`, set `LEDGER_BACKEND_SERVER_ADDRESS`

# Server bind address (default: 127.0.0.1)
# LEDGER_BACKEND_SERVER_ADDRESS="127.0.0.1"

# Server port (default: 50059)
# LEDGER_BACKEND_SERVER_PORT=50059

# Data directory for application files (default: data/)
# LEDGER_BACKEND_SERVER_DATA_DIR="data/"

# Logging level: OFF, ERROR, WARN, INFO, DEBUG, TRACE (default: WARN)
# LEDGER_BACKEND_SERVER_LOG_LEVEL="WARN"

# Enable TLS for gRPC server (default: false)
# LEDGER_BACKEND_SERVER_TLS_ENABLED=false

# TLS certificate path (PEM format, required when TLS is enabled)
# LEDGER_BACKEND_SERVER_TLS_CERT_PATH="/path/to/tls/cert.pem"

# TLS private key path (PEM format, required when TLS is enabled)
# LEDGER_BACKEND_SERVER_TLS_KEY_PATH="/path/to/tls/key.pem"

# SQLite database file path (default: personal_ledger.db)
# LEDGER_BACKEND_SERVER_DATABASE_PATH="./personal-ledger-dev.db"
